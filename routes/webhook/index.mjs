import express from "express";
const webhooksRouter = express.Router();

// TODO: ADD any routes for your Backend Webhook routes here (or break them down into separate files and import them into here)

export default webhooksRouter;
